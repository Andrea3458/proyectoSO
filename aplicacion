#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/stat.h>
#include <syslog.h>

int main() {
	if (access("./proy1.ini", F_OK) != 0) {
		syslog(LOG_ERR, "El archivo de configuración no existe.");
		exit(EXIT_FAILURE);
	}
	pid_t pid = fork();
	return 0;
}
